{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "STATICPACK",
    "buildCommand": "npm run build",
    "outputDirectory": "dist"
  },
  "deploy": {
    "runtime": "STATIC"
  }
}